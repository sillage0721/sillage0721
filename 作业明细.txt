1.设计一个类，计算两个数的加减乘除。
2.设计一个类，定义一个50个元素的浮点数组，随机生成50个数, 赋给数组，计算总成绩和平均成绩。
3.
（1）修改把大象装进冰箱的行为，增加判断冰箱是否打开，大象是否装进冰箱，
  冰箱是否关闭的三个判断，最终设计一个可以编译的类，方法体里执行具体
  过程直接用System.out.println()表示。
（2）按照今天的上课内容，完善2类。
4. 构造一个长方体类，求长方体的体积，写两个构造方法，一是没有参数，
    二是包含三个参数（长，宽和高），创建对象且输出对象体积。
5.针对描述汽车的任务，设计汽车类及其相关子类，根据汽车的共性和个性，体现
  重写，上转型，多态等相关的特点。
6.任意列举三个不同对象（不包括已讲过了），抽象出公共的一个行为，
  分别按照抽象类和接口的方法，完成三个对象对应的三个类的设计，实现
  对应的抽象方法和接口，实现三个对象对公共行为的调用，并用Lambada
  表达式对接口变量赋值，实现接口方法。

7.设计一种场景，具体用内部类，匿名类的两种方式和异常处理来设计这个大类，场景自定义。
8.把平时作业的内容，按照次数，用GUI（JFrame)的形式组织，布局和界面风格自己定义。
9.写一个日志文件，记录每次操作的内容和时间，并可以读取日志文件，显示每次操作的内容
  和时间。
大作业：把以上9个内容的功能，用GUI组织在一起，通过选择实现不同功能。
类命名规则：
C+类描述+班级后两位+学号后四位
例子：CLog030330
大作业要求：
（1）描述通过设计那些类完成那些功能，
         描述每个类和接口的详细功能
（2）调试和运行小结，程序注释详细
（3）所有类和接口都在一个文件中，源程序就是一个文件。
        文件名：CMain030330.java
附加题：
用线程方式，设计一个并发执行程序，同时执行上面9个内容程序。